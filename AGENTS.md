# EPGS Agent 执行规范

本文件适用于仓库根目录及全部子目录。所有参与本项目的自动化 Agent、编码助手和人工开发者都必须遵守。

## 1. 基本原则

1. GitHub Issue 是开发任务的唯一需求来源。开始工作前必须完整阅读 Issue 正文、验收标准、关联依赖和最新评论。
2. 一个 Issue 对应一个独立分支和一个 Pull Request。不得在同一分支混做多个 Issue，也不得在一个 PR 中夹带无关修改。
3. 禁止直接向 `main` 推送开发代码。只有项目所有者 `@xrunda` 负责审核和合并 PR；Agent 不得自行合并、关闭或跳过审核。
4. 需求不明确、接口依赖缺失或验收标准彼此冲突时，必须在 Issue 中说明阻塞点并等待确认，不得擅自扩大或改变业务范围。
5. 优先做满足 Issue 验收标准的最小实现。未经授权不得顺便重构、升级大版本依赖或引入新的基础设施。

## 2. 标准开发流程

### 2.1 领取与检查 Issue

- 确认 Issue 为 Open 状态，并检查其里程碑、优先级、依赖 Issue 和完成定义。
- 检查是否已存在关联分支或未关闭 PR，避免重复开发。
- 在开始编码前，将 Issue 的目标、非目标、输入输出、测试要求整理成个人执行清单。
- 若依赖尚未合并，可以基于 mock/fixture 独立开发；不得复制另一未合并分支的实现后形成两个事实来源。

### 2.2 创建分支

每个 Issue 必须从最新的 `origin/main` 创建新分支：

```bash
git fetch origin
git switch main
git pull --ff-only origin main
git switch -c issue/<issue-number>-<short-kebab-slug>
```

示例：

```text
issue/5-keyword-matching-engine
issue/9-monitor-dashboard
```

规则：

- 分支名必须包含 Issue 编号。
- 不得从其他功能分支创建新 Issue 分支，除非 Issue 明确记录了堆叠 PR 方案。
- 不得在共享分支执行 force push、历史重写或删除他人提交。

### 2.3 开发与自检

- 严格遵循 Issue 的“实现范围”和“不在范围”。发现额外需求时新建或建议新建 Issue，不得隐式扩展当前 PR。
- 先补测试或测试样例，再实现能够通过测试的最小代码；修复缺陷必须包含回归测试。
- 使用脱敏 fixture、mock adapter 或合成数据。严禁提交真实患者姓名、住院号、报告正文、数据库快照、账号、Token、密码或连接串。
- PACS/RIS/HIS 集成默认只读；未经项目所有者明确书面授权不得执行写库、删库、结构变更或生产数据回填。
- 关键词分级是管理“关注等级”，不是正式诊断或病情严重程度。未审核报告必须保留“仅用于监测，不作为正式诊断”的提示。
- 后端负责生产环境的同步、匹配、鉴权和审计；不得把关键业务判断只放在前端。
- 更新接口、配置、数据结构或运行方式时，同步更新文档、示例和迁移说明。

提交前至少运行仓库提供的格式化、静态检查、单元测试和构建命令。若仓库暂未提供统一命令，应在 PR 中列出实际执行的验证方式和结果。

### 2.4 提交规范

提交应小而清晰，采用 Conventional Commits：

```text
feat(scope): summary
fix(scope): summary
test(scope): summary
docs(scope): summary
chore(scope): summary
```

- 每个提交只表达一个可说明的变更目的。
- 提交信息或 PR 正文必须引用 Issue 编号。
- 不得提交生成缓存、编辑器状态、日志、临时导出文件或敏感数据。
- 不得使用 `--no-verify` 绕过质量检查，除非项目所有者在 Issue 中明确批准。

### 2.5 创建 Pull Request

完成实现并推送 Issue 分支后，必须创建 PR：

```bash
git push -u origin issue/<issue-number>-<short-kebab-slug>
gh pr create --base main --head issue/<issue-number>-<short-kebab-slug>
```

- 开发未完成时创建 Draft PR；满足验收标准和 DoD 后再标记 Ready for review。
- PR 标题应清楚表达交付结果，正文使用仓库 PR 模板，并包含 `Closes #<issue-number>`。
- PR 必须列出改动范围、明确未改内容、验证命令与结果、风险、回滚方案以及 UI 截图（如适用）。
- 数据库迁移、接口不兼容或配置变更必须在 PR 中醒目标注。
- Agent 只能响应评审意见并继续推送到同一 Issue 分支；不得创建重复 PR。
- Agent 永远不得点击 Merge、启用自动合并或直接关闭 Issue。由项目所有者 `@xrunda` 完成最终合并；Issue 通过 `Closes #...` 在合并后自动关闭。

## 3. 测试与验收门禁

PR 在请求合并前必须同时满足：

- Issue 中的每条验收标准均已逐项勾选，或在 PR 中给出尚未满足的明确说明。
- 新增或修改逻辑具有自动化测试，覆盖成功、失败、空数据、边界值、重复请求和无权限场景中适用的部分。
- lint、typecheck、unit/integration test、build 等仓库现有 CI 全部通过。
- 涉及 UI 时完成目标分辨率验证、加载/空/错状态、键盘操作和非纯色信息表达。
- 涉及同步或写入时验证幂等、重试、故障恢复和重复执行。
- 涉及患者数据时验证最小化返回、脱敏、授权和审计，且日志无敏感正文。
- 涉及数据库迁移时验证升级、回滚、兼容性和索引影响。
- 不得以“本地无法测试”为由声明完成；确有外部环境阻塞时保持 Draft，并在 Issue/PR 中列出复现步骤和所需条件。

## 4. 评审反馈处理

- 每条评审意见都需要回复：已修复并给出提交、解释为何不修改，或请求进一步确认。
- 修复评审问题时继续使用原分支和原 PR，并补充对应测试。
- 修改完成后重新运行受影响的完整测试集，更新 PR 验证结果。
- 不得删除、隐藏或标记已解决尚未实际处理的评审意见。

## 5. 冲突与异常处理

- 分支落后时优先将最新 `origin/main` 合入当前 Issue 分支；解决冲突后重新运行测试。
- 禁止对 `main` 使用 rebase、force push、reset 或其他改写历史操作。
- 发现安全漏洞、真实患者数据或凭据泄露时立即停止开发，不要在公开 Issue/日志中复制敏感内容，并通知项目所有者处理。
- CI 或外部系统不可用时记录具体失败、日志位置和已尝试措施；不得伪造通过结果。

## 6. Agent 最终交付格式

Agent 完成一个 Issue 时，最终回复至少包含：

1. Issue 编号与目标。
2. 分支名称。
3. PR 链接。
4. 主要实现内容。
5. 实际执行的测试命令和结果。
6. 未完成事项、风险或需要项目所有者确认的内容。

没有 PR 链接的开发任务不得宣称“已完成”。

## 7. 初始化例外

仓库建立时没有默认分支，根级 `AGENTS.md`、PR 模板和 CODEOWNERS 作为首次初始化提交直接创建 `main`。该例外仅限这一次仓库初始化；从首个功能 Issue 开始，所有修改均必须遵循“一 Issue、一分支、一 PR、项目所有者合并”的流程。
