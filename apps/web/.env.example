# apps/web local environment. Copy to apps/web/.env for local dev.
# API requests are relative to the page's own origin (see apps/web/src/authApi.ts) -
# local dev relies on Vite's dev-server proxy (see vite.config.ts) to forward
# /api and /health to the api process, so no base-URL env var is needed here.
PORT=5173
